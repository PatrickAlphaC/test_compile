[project]
name = "test-compile"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "snekmate>=0.1.0",
    "vyper>=0.4.0",
]
